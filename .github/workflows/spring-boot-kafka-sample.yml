name: spring-boot-kafka-sample

on:
  push:
    paths:
      - "spring-boot-kafka-sample/**"
    branches: [ master ]
  pull_request:
    paths:
      - "spring-boot-kafka-sample/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
          fetch-depth: 0
    - name: Set up JDK
      uses: actions/setup-java@v3.9.0
      with:
        java-version: '17'
        distribution: 'adopt'
        cache: 'maven'
    - name: Build with Maven
      run: |
        cd spring-boot-kafka-sample
        mvn -B package --file pom.xml
