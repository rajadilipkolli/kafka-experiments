name: analytics-spring-cloud-streams-kafka-consumer

on:
  push:
    paths:
      - "analytics-spring-cloud-streams-kafka-consumer/**"
    branches: [ master ]
  pull_request:
    paths:
      - "analytics-spring-cloud-streams-kafka-consumer/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
      with:
          fetch-depth: 0
    - name: Set up JDK
      uses: actions/setup-java@v3.1.0
      with:
        java-version: '17'
        distribution: 'adopt'
        cache: 'maven'
    - name: Build with Maven
      run: |
        cd analytics-spring-cloud-streams-kafka-consumer
        mvn -B package --file pom.xml
