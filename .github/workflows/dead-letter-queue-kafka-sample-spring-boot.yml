name: kafka-dead-letter-publishing-spring-boot

on:
  push:
    paths:
      - "kafka-dead-letter-publishing-spring-boot/**"
    branches: [ master ]
  pull_request:
    paths:
      - "kafka-dead-letter-publishing-spring-boot/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "kafka-dead-letter-publishing-spring-boot"
    steps:
    - uses: actions/checkout@v3
      with:
          fetch-depth: 0
    - name: Set up JDK
      uses: actions/setup-java@v3.9.0
      with:
        java-version: '17'
        distribution: 'adopt'
        cache: 'maven'
    - name: Build with Maven
      run: ./mvnw -B verify --file pom.xml
